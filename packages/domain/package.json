{
  "name": "@every-player/domain",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^4.1.12",
    "@every-player/types": "workspace:*"
  },
  "devDependencies": {
    "@every-player/typescript-config": "workspace:*",
    "@every-player/eslint-config": "workspace:*",
    "@every-player/prettier-config": "workspace:*",
    "typescript": "~5.9.3"
  }
}
